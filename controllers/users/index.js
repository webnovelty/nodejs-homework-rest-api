
const { add } = require("./add")
const { login } = require("./login")
const { getCurrent } = require("./getCurrent")
const { logout } = require("./logout")


module.exports = {
	add, login, getCurrent, logout,
}